[project]
name = "anonify"
version = "0.1.0"
description = "FastAPI project for text sanitization and anonymization."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.119.1",
    "pydantic>=2.12.3",
    "requests>=2.32.5"
]
